version: '3.8'

services:
  app:
    build: .
    container_name: strocz-dashboard
    restart: unless-stopped
    ports:
      - "8080:80"
    # Uncomment below if you want to use with a reverse proxy
    # expose:
    #   - "80"
    # networks:
    #   - web

# Uncomment if using external network for reverse proxy
# networks:
#   web:
#     external: true
